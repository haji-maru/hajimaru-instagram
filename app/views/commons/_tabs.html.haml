.tabs
  .tabs-item{class: "#{add_active_class(posts_path)}"}
    = link_to 'All Post', posts_path, data: { turbolinks: false }
  .tabs-item{class: "#{add_active_class(timeline_path)}"}
    = link_to 'Timeline', timeline_path, data: { turbolinks: false }